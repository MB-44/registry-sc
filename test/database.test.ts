// src/test/database.test.ts

describe("Database test suite", () => {
    it("should connect to database or do something", () => {
      // A basic assertion
      expect(1 + 1).toBe(2);
    });
  });
  